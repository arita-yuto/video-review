# =====================================================================
#  VideoReview - Environment Variables Example
# =====================================================================
#  Define all environment variables required for running VideoReview.
#  Only fill in what you need depending on which features you will use.
# =====================================================================


# ---------------------------------------------------------------------
#  Public settings used on the frontend (requires NEXT_PUBLIC_ prefix)
# ---------------------------------------------------------------------

# The title displayed on the login screen and header.
NEXT_PUBLIC_VIDEO_REVIEW_TITLE="VideoReview"

# A short description shown on the login screen.
NEXT_PUBLIC_VIDEO_REVIEW_DESC="Internal Video Review Tool"

# Background image for the login screen.
# If empty, a solid black background will be used.
# Example: "/images/login-bg.jpg"
NEXT_PUBLIC_VIDEO_REVIEW_LOGIN_BG=""

# ---------------------------------------------------------------------
#  VideoReview
# ---------------------------------------------------------------------
# API token required for uploading videos via the upload API.
# Example: "supersecrettoken123"
VIDEO_REVIEW_API_TOKEN=""

# Storage backend to use for uploaded video files.
# Options: "local" (local filesystem), "s3" (AWS S3), "nextCloud"
VIDEO_REVIEW_STORAGE="local"

# Local Storage Configuration
# Root directory within local for storing uploaded files.
VIDEO_REVIEW_LOCAL_ROOTDIR="/storage"

# ---------------------------------------------------------------------
#  JIRA integration
# ---------------------------------------------------------------------

# Base URL for linking to JIRA issues.
# Example: https://yourcompany.atlassian.net
VIDEO_REVIEW_JIRA_BASE_URL=""

# JIRA API Token (generate from your JIRA account settings).
VIDEO_REVIEW_JIRA_API_TOKEN=""

# JIRA project key to which new issues will be created.
# Example: "GAMEDEV"
VIDEO_REVIEW_JIRA_PROJECT=""

# User assigned automatically to newly created issues
# (JIRA email).
VIDEO_REVIEW_JIRA_ASSIGNEE_USER=""

# Issue types to show in the UI when creating JIRA issues.
# Example: "task" or "bug" (Issue Type name)
NEXT_PUBLIC_VIDEO_REVIEW_JIRA_ISSUE_TYPE_TASK=""
NEXT_PUBLIC_VIDEO_REVIEW_JIRA_ISSUE_TYPE_BUG=""

# ---------------------------------------------------------------------
#  Slack Notifications
# ---------------------------------------------------------------------

# Slack Bot User OAuth Token (begins with "xoxb-").
VIDEO_REVIEW_SLACK_API_TOKEN=""

# Slack channel name or channel ID where notifications will be posted.
# Example: "C00XXXXXX"
VIDEO_REVIEW_SLACK_POST_CH=""

# Slack Team Domain Name
# Once set up, you can jump to the posted post from VideoReview.
# Example: "Game.dev"
VIDEO_REVIEW_SLACK_TEAM=""

# ---------------------------------------------------------------------
#  Webhook Notifications
# ---------------------------------------------------------------------

# Example: slack, teams, ...
VIDEO_REVIEW_WEBHOOK_TARGET=""

# Example: slack: https://hooks.slack.com/services/....
# Example: teams: https://azure.com:443/workflows/xxxxxxxxxxxxxxxxxxxxxxx/triggers/....
VIDEO_REVIEW_WEBHOOK_URL=""

# ---------------------------------------------------------------------
#  Email Notifications
# ---------------------------------------------------------------------

# Enable or disable email notifications globally.
# When set to "false", no emails will be sent and SMTP will not be used.
# Default: false
VIDEO_REVIEW_EMAIL_ENABLE="false"

# Enable strict TLS certificate verification.
# Set this to true only if your SMTP server uses a valid, trusted certificate.
VIDEO_REVIEW_SMTP_TLS_STRICT="false"

# SMTP server host used to send emails.
#
# Examples:
#   smtp                 (Docker Compose service name)
VIDEO_REVIEW_SMTP_HOST="smtp"

# SMTP server port.
# Common values:
#   25    : internal SMTP relay (no TLS)
VIDEO_REVIEW_SMTP_PORT="25"

# Sender email address used for all notifications.
# This should be a valid address accepted by the SMTP relay.
# Example:
#   noreply@videoreview.dev
#   noreply@example.com
VIDEO_REVIEW_EMAIL_FROM="noreply@videoreview.dev"

# ---------------------------------------------------------------------
#  Database
# ---------------------------------------------------------------------

# PostgreSQL connection string used by Prisma.
# Example: "postgresql://user:password@localhost:5432/videoreview"
DATABASE_URL="postgresql://user:password@localhost:5432/videoreview"

# ---------------------------------------------------------------------
#  Docker
# ---------------------------------------------------------------------

# Docker only: host path for persistent storage
# If empty, Docker named volume will be used.
DOCKER_HOST_STORAGE="videoreview-storage"

# Docker only: host path for LLM model files used by Ollama.
# If empty, Docker named volume will be used.
DOCKER_HOST_LLM_MODELS=""

# ---------------------------------------------------------------------
#  AWS
# ---------------------------------------------------------------------

# AWS S3 Bucket name for storing uploaded files.
# Example: "videoreview-bucket"
S3_BUCKET="videoreview-bucket"

# AWS S3 Region.
# Example: "us-east-1"
S3_REGION=""

# AWS S3 LocalStack endpoint (for local development).
# Leave empty when not using LocalStack.
# Example: "http://localstack:4566"
S3_LOCALSTACK_ENDPOINT=""

#  AWS Credentials
AWS_ACCESS_KEY_ID="test"
AWS_SECRET_ACCESS_KEY="test"

# =====================================================================
# nextCloud Storage Configuration
# =====================================================================
# Base URL of the nextCloud/OwnCloud instance (e.g., "https://cloud.yourcompany.com/nextcloud").
VIDEO_REVIEW_NEXTCLOUD_BASE_URL=""

# Username for nextCloud/OwnCloud authentication.
VIDEO_REVIEW_NEXTCLOUD_USERNAME=""

# Password or App Password for nextCloud/OwnCloud authentication.
VIDEO_REVIEW_NEXTCLOUD_PASSWORD=""

# Root directory within nextCloud for storing uploaded files.
VIDEO_REVIEW_NEXTCLOUD_ROOTDIR=""


# =====================================================================
# LLM Configuration (AI auto-annotation)
# =====================================================================

# LLM provider used by AI summaries, auto-annotation and chat search.
# Options: "claude", "openai", "gemini", "ollama"
# Leave empty to disable LLM features.
VIDEO_REVIEW_LLM_PROVIDER=""

# API key for the provider (required for "claude", "openai" and "gemini").
# Claude: https://console.anthropic.com/  OpenAI: https://platform.openai.com/  Gemini: https://aistudio.google.com/
VIDEO_REVIEW_LLM_API_KEY=""

# Base URL for the "ollama" provider (default: http://localhost:11434). Ignored by the others.
VIDEO_REVIEW_LLM_BASE_URL="http://localhost:11434"

# Model name.
# Claude examples : "claude-haiku-4-5-20251001", "claude-sonnet-4-6"
# OpenAI examples : "gpt-5-mini", "gpt-5"
# Gemini examples : "gemini-3.6-flash"
# Ollama examples : "llama3.1:8b", "gemma3:12b"
VIDEO_REVIEW_LLM_MODEL=""

# =====================================================================
# MCP Server (natural-language search from the in-app chat and AI assistants)
# =====================================================================

# The app serves MCP at /api/v1/mcp; AI agents connect there with an API token.
# Team notes for the search guide are written on the admin screen (Integrations > MCP).

# =====================================================================
# VCS Integration (PR / commit diff display between video revisions)
# =====================================================================

# VCS provider to use. Only "github" is implemented.
# The settings below count only when this is "github". The admin screen can pick the provider and set these too; saved values win.
VIDEO_REVIEW_VCS_PROVIDER=""

# GitHub settings (required when provider is "github")
VIDEO_REVIEW_VCS_GITHUB_OWNER=""
VIDEO_REVIEW_VCS_GITHUB_REPO=""
VIDEO_REVIEW_VCS_GITHUB_TOKEN=""

# Default branch to query commits from.
VIDEO_REVIEW_VCS_BRANCH="main"

# =====================================================================
#  Deprecated: set these on the admin screen instead (Administration > General).
#  They are still read while nothing is saved there, and the saved value wins.
# =====================================================================

# Guest login. Only "false" turns it off; the tab disappears with it.
VIDEO_REVIEW_ALLOW_GUEST="true"

# The tab the login screen opens on: "guest", "jira" or "password".
NEXT_PUBLIC_VIDEO_REVIEW_LOGIN_DEFAULT_TYPE="guest"

# URL scheme that opens a scene in the editor. Default: the bundled launcher's "videoreview://open?scene={scenePath}".
NEXT_PUBLIC_VIDEO_REVIEW_URL_SCHEMA=""

# Widths of the downscaled variants the video processing makes, comma-separated.
NEXT_PUBLIC_VIDEO_REVIEW_RESOLUTION_PRESETS="480,720,1080"

# Size of one chunk of a video upload, in MB. Lower it when a proxy in front of the server
# refuses bodies of this size (nginx allows 1MB unless configured). S3 needs at least 5.
VIDEO_REVIEW_UPLOAD_CHUNK_MB="16"
